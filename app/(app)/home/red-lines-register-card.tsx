// Owned by ticket 03 (red-lines CRUD). Replace this placeholder with a real
// preview/link into the red-lines list — home/page.tsx imports this
// component as-is and should not need to change.
export function RedLinesRegisterCard() {
  return (
    <section className="register-card">
      <p className="ledger-ref tabular">§ RED LINES</p>
      <h2 className="register-heading">Your red lines</h2>
      <p className="register-body">
        Your personal list of dealbreakers will live here and drive what
        Redline flags.
      </p>
    </section>
  );
}
