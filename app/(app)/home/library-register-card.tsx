// Owned by ticket 11 (document library). Replace this placeholder with a
// real preview/link into the library — home/page.tsx imports this component
// as-is and should not need to change.
export function LibraryRegisterCard() {
  return (
    <section className="register-card">
      <p className="ledger-ref tabular">§ LIBRARY</p>
      <h2 className="register-heading">Library</h2>
      <p className="register-body">
        Documents you&rsquo;ve reviewed will be saved here so you can revisit
        them.
      </p>
    </section>
  );
}
